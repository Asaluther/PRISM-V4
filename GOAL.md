# PRISM — 阶段目标清单（Goal Roadmap）

> 2026-09-07 制定 | 基于 V4 修复轮发现（results/V4_FINDINGS.md）
> 北极星：**终端私有 AGI** —— 个人设备可运行、低功耗、可在线学习、数据不出端的通用智能架构。
> 路线：原理验证（V4）→ 规模与域（V5）→ 在线学习范式（V6）→ 终端形态（V7）

---

## 当前坐标（2026-09-11 晚，V6 关闭后更新）

- **论文已发布**：AiraXiv 2609.0010（2026-09-10，v2.1）
- **V6 已关闭**（终局文档 `results/V6_CLOSURE.md`）：主线 W&B 局部规则 FAIL 终判（2.4-3.7× 结构性差距，用户决策按预案关闭）；备线完成端侧 demo + 骨架约束 + 遗忘基准 + 能耗基准
- **今日新增科学资产**：遗忘基准（域内个性化无空间=否定性结果 / 匹配适应下 PCN 遗忘更多 / ΔW 小≠遗忘少——第二涌现性质重定为「参数经济性」）；T4 能耗（服务端最优 / 终端 b1 能耗 2× 为短板）
- **两次基线勘误**：demo 与骨架系列的 TF 臂用了次优 checkpoint（PPL 557 vs 公平版 215）——demo 修正口径 PCN +58.5% vs TF +5.4%（优势保持，「TF 退化」叙事撤销）；ΔW 约束幅度对公平基线稳健（1.91×）
- **下一步（V7 前置清单，按序）**：① ~~batch1 kernel 优化~~ **已完成（T5：CUDA Graph 化，b1 达 TF-graph 102%/91.5%，能耗反超 -28%）** ② ~~4bit 量化~~ **已完成（T6：TF int4-g64 近无损；PCN 配方 = int4-g64 + W_res 保 fp16 近无损；发现「W_res 非加性残差主干 = PCN 量化脆弱点」）** ③ ~~MQAR/recall 基准~~ **已完成（否定性/边界结论：四阶段含混淆排查——玩具尺度 D64-128 四臂均不解出标准 MQAR；阶段一~三的「门控 0.115」被众数值捷径（天花板 0.1263）完全解释，值唯一版复核精确回到随机（LESSONS 教训 10）；复制优势不可外推到键值绑定，召回主张锚定复制任务；recall-效率前沿留待规模扩展期 d256+ 重试，详见 mechanism/MQAR_BENCHMARK.md）** ④ ~~流式适应 demo~~ **已完成（STREAMING_REPORT：跨域冷启动流 PCN 在线 +24.0% vs TF -80.9%，ΔW 约束三场景再现 1.87×；域内已收敛流双输——流式仅冷启动有效）** ⑤ 论文 v2.2（并入今日全部结果；注意 MQAR 为否定性/边界结论，不得写成优势）
- 规模扩展（200M+、完整 Mamba/RWKV）搁置待论文外部反馈（2026-09-11 用户决策）

---

## 阶段总览

| 阶段 | 主题 | 预算 | 关键判据（go） | 失败出口 |
|------|------|------|----------------|----------|
| V4.x | 收尾验证与决策 | ~1 周 / ~8 GPU·h | 多 seed 排序稳定 + 反馈去留有据 | 排序被推翻 → 域伪影结论，V5 提前 |
| V5 | 规模与真实域 | ~1-2 月 / ~80 GPU·h | 真实语料上 token 效率 ≥ Transformer | 差距消失 → 记录否定性结论，评估 HMN/SAPN |
| V6 | 在线学习范式 | ~2-3 月 / ~100 GPU·h | 局部学习规则达 BP 效率的 80% 且抗遗忘 | 不达 → 转向稀疏高效推理路线 | **已关闭（2026-09-11，FAIL→备线，见 V6_CLOSURE.md）** |
| V7 | 终端形态 | 远期 | 端侧持续学习 demo | 启动条件已改（见 V7 节） |

---

## V4.x — 收尾验证与决策（本周）

**目标**：把修复轮的「单 seed、单域、弱模型签名」三个软肋补掉，并对反馈连接做出有据决策。

| # | 任务 | 预算 | 产出 |
|---|------|------|------|
| 1 | 多 seed 复验：no_feedback / no_gating / transformer 各 5 seed | ~2.5 GPU·h | 排序稳定性 + 方差（PPL 均值±std） |
| 2 | no_feedback 模型签名分析（复用 analyze_v2.py 换 checkpoint） | 0.5 h | 强模型的 CKA / 门控熵 / 误差-惊讶相关 |
| 3 | 复制任务优势正式化：多长度（16/32/64）× 多 seed（5）× 步数曲线 | ~1 GPU·h | 「定性不同能力」证据（V4_PLAN §6.3 条件 3） |
| 4 | 反馈连接判定实验：(a) W_td 深层监督 (b) 2 步迭代推断 (c) 裁撤 —— 三臂对比 | ~3 GPU·h | 反馈去留决策 + 决策记录（MEMORY/决策点.md） |
| 5 | d_gate ∈ {16,32,64} × topk ∈ {16,32,64} 敏感性扫描（no_feedback 版） | ~2 GPU·h | 架构健壮性边界 |
| 6 | V4 结论文档：对照 V4_PLAN §6.3 四条件逐条判定 | 0.5 天 | V4 最终结论（含否定性条款） |

**Go 判据**：多 seed 下 PCN 最强变体仍 ≥ Transformer（或差距在方差内）；反馈决策三臂中有一臂显著优于裁撤或确认裁撤最优。
**失败出口**：排序被推翻 → 结论改写为「TinyStories 简单域伪影」，直接进 V5 验证真实域。

---

## V5 — 规模与真实域（解锁「可推广性」）

**目标**：回答「误差门控的优势在真实语料和更大规模上是否存续」，并兑现计算复杂度优势。

| # | 任务 | 说明 |
|---|------|------|
| 1 | 域扩展：WikiText-103（先）→ OpenWebText-2（后），同预算协议 | 解除 LESSONS 教训 1（小域不可推广） |
| 2 | 规模阶梯：d ∈ {256, 384, 512} × 3 配置，20K 步 | 绘制 PCN 的 loss-参数 scaling 曲线，对照 §2.2「偏离 power law」指标 |
| 3 | 门控 kernel 化：torch.compile → 手写 Triton fused Top-K 门控 | 吞吐目标 ≥ attention 的 80%；效率证据线核心 |
| 4 | MQAR / recall 正式基准 + recall-效率权衡图 | 把 PCN 放上 BASED 的「回忆-效率」前沿，对标 Mamba/RWKV/Transformer |
| 5 | 同能力能耗对比：达到同 PPL 所需 J/token（训练+推理两口径） | 终端愿景的核心量化证据 |
| 6 | 消融移植：在真实域重跑反馈三臂 + 门控/attention 聚合对比 | 验证 V4.x 结论的域稳定性 |

**Go 判据**：真实语料上达到同 PPL 的 token 效率 ≥ Transformer（即同预算 PPL 不劣于）或能效优势 > 20%。
**失败出口**：优势消失 → 「误差门护是简单域伪影」的否定性结论同样有价值（缩小设计空间，评估 HMN/SAPN，更新 V4_PLAN）。

---

## V6 — 在线学习范式（终端愿景的第一块基石）【已关闭 2026-09-11】

> 终局判定与全部数据见 `results/V6_CLOSURE.md`。以下为原始计划，留档。

**Go 判据**：局部规则达到 BP 最终性能的 ≥80% 且遗忘率 < 冻结基线的 2 倍衰减。
**实际结局**：主线 FAIL 终判（`local_rule_level2_FINAL.json`）；V6#2 遗忘基准已于关闭日
首次执行（PCN 高可塑性端，非低遗忘端）；V6#3/#4 未独立执行。用户决策按失败出口转
「稀疏高效推理 + 样本效率微调」路线，不追非 W&B 复活实验。

| # | 任务 | 说明 |
|---|------|------|
| 1 | 局部学习规则实现：以误差 e 为局部信号的更新规则替代全局 BP | 等效性验证：同任务下局部 vs BP 的收敛曲线 |
| 2 | 灾难性遗忘基准：交替任务流（A→B→A）下的保持率 vs 冻结基线 | 在线学习的必要条件 |
| 3 | 小样本在线适应：新任务 few-shot 适应速度 vs Transformer ICL | 「学习元能力」证据 |
| 4 | 流式学习原型：continual data stream 上的「边用边学」demo | 终端形态的最小可行证据 |
| 5 | 安全线：在线学习下的安全边界（可塑性-稳定性 + 行为约束） | 私有 AGI 的安全设计输入 |

---

## V7 — 终端形态（启动条件已改：不再依赖「V6 通过」）

> 2026-09-11 修订：V6 主线 FAIL 后按失败出口转「稀疏高效推理 + 样本效率微调」路线，
> V7 以该路线为基。当前两大前置短板：终端 b1 吞吐 60% / 能耗 2×（t4_energy.json）。

| # | 任务 | 说明 |
|---|------|------|
| 0 | **batch1 kernel 优化（CUDA Graph/fused）** | 终端吞吐/能耗双短板，最高优先 |
| 1 | 稀疏激活扩展：门控稀疏性 × 激活稀疏性（融合 SAPN 思想：事件驱动） | 对标人脑「大容量、极稀疏、低功耗」设计空间 |
| 2 | 端侧压缩：量化（4bit）+ 蒸馏至 0.5-2B 稀疏参数 | 手机 ~8W / 笔记本 ~30W 功耗预算内的推理 |
| 3 | 端侧 demo：PC/手机上运行 + 本地持续学习 + 数据不出端验证 | 北极星的第一个可见里程碑 |
| 4 | 私有 AGI 安全框架：所有者绑定、可审计、在线学习的护栏 | 「足够安全」的具体化 |

---

## 贯穿原则（每阶段适用）

1. **效率证据线**：每轮实验更新 `efficiency.json`（吞吐 / VRAM / 功耗 / J/token）——终端愿景的纵向数据
2. **否定性结果也是结果**：每个 Go/No-Go 判据失败时，结论写入 V4_PLAN 式文档，不沉没成本
3. **决策入档**：重大取舍（如反馈裁撤）记录于 `MEMORY/决策点.md`（本机）+ 报告文档（项目内）
4. **同预算协议**：任何架构对比固定 batch×seq×steps 的 token 总量，杜绝 1/16 预算错配复发
5. **单变量消融**：架构对比每次只动一个机制（三臂模式：full / 去 X / 换 X 的对照）

---

## 已知风险（继承自 V4_PLAN + 本轮新发现）

- TinyStories 优势可能是域伪影（V4.x#1 + V5#1 双重检验）
- 反馈连接的失败可能预示「预测编码假说在语言建模上的根本困难」（V4.x#4 定向排查）
- 门控 kernel 化不达 80% 吞吐 → 复杂度优势停留在纸面（V5#3）
- 局部学习规则的理论-实践差距（V6 最大不确定性，Whittington-Bogacz 是小网络验证）
- 单人单卡节奏：预算按 RTX 4080 16GB 折算，V5 起需警惕数据预处理的内存瓶颈（tinystories.py 教训）

*最后更新：2026-09-11 | 坐标：V6 关闭（终局见 results/V6_CLOSURE.md），V7 前置清单已列（batch1 优化居首），规模扩展搁置待论文外部反馈*
