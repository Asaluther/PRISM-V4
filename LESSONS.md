# PRISM V4 — Lessons from V1-V3

> 本文件是 V1-V3 的唯一参考。不包含任何架构数据。
> V4 工作期间不主动引用原 PRISM 目录的任何文件。

---

## 元教训

### 1. 小规模验证不可靠
- 12 层 d=256 的实验结果**不能**推广到架构层面的结论
- 小模型上所有机制的表现都受"正则化效应"干扰
- 从 10M 参数到真正的新架构，验证体系完全不同

### 2. 从 Transformer 修补出发 → 回到 Transformer
- 三次（V1/V2/V3）都是"在 Transformer 上做减法/替换/门控"
- 每次最终都退回到"Transformer + 某种正则化"
- **要设计新架构，必须从不同的基线出发，而非从 Transformer 去掉东西**

### 3. "机制退化"是假设错误的信号
- 如果一个精心设计的机制在训练中自动退化为更简单的版本（如维度级门控退化为标量），说明架构假设本身有问题
- 这不是"需要调参"，是"方向错了"

### 4. 训练超参可以主导架构变化
- 训练步数 > 正则化效应 > 差异化门控 > 核心假设的贡献
- PPL 变化的归因必须排除超参干扰，否则会把训练技巧的功劳归给架构

### 5. 对照组决定结论的有效性
- 没有严格对照的实验结论不可信
- 门控 vs Dropout 的对照是 V3 中最有价值的实验设计

### 6. 否定性结果也是结果
- 证明"此路不通"和证明"此路可行"价值相同
- 20 个实验的系统否定比 1 个实验的偶然成功更有意义

### 7. 因果性是语言模型实验的公理，不是配置项（2026-09-08，V5 期间）
- **情境**：任何「预测下一 token」的架构对比实验
- **必须先做**：所有跨位置交互（attention / 门控 / 任何聚合）显式因果化（mask j>i）；并用「作弊探测器」验收——训练后的模型在 held-out 真实文本上 **top-1 准确率 >50% 或 PPL 异常低（真实语料 <5），几乎必然非因果**
- **反例**：2026-09-08 发现 `nn.MultiheadAttention` 未传 attn_mask、PCN 门控无掩码——V4 全部对比（含 55% PPL 优势、复制任务 5/5 vs 0/5）实为「两个双向模型比谁更会抄未来」。22M 模型在维基文章上 top-1 准确率 99.6% 才暴露
- **要点**：当一个「惊人优势」出现时，**先怀疑作弊通道，再相信架构创新**。WikiText 上 PPL 2.24 这种「好到不可能」的数字是最好的报警器；TinyStories 上 1.1 vs 2.5 的优势看起来「合理」，反而骗过了两天
- 关联教训 5：此条是其极端形式——对照组与实验组共享同一个致命缺陷时，对照形同虚设
- **补充实例（2026-09-10，第三架构）**：新实现的衰减注意力 dt 方向定义反了（掩码允许未来/屏蔽过去）——泄漏版在 TS 上 PPL 1.71「好到不可能」，探测器（top-1 准确率 91.8%）立即抓住。**两个附加教训**：①周期任务的 sanity 校准对泄露不敏感（周期任务里未来=过去）——校准任务必须用泄露敏感的（复制任务/真实语料+打乱测试）②清除脏数据要彻底（TS 删了 WT 漏删，差点污染汇总）

### 9. 自建对照结构必须先用已知结论校准对照格（2026-09-09，V5 期间）
- **情境**：为分解实验在外部重写一个「简化版」模型结构（而非在原实现上加开关）
- **必须先做**：跑「已知结论的对照格」——把已验证的配置在新结构里复现，
  结果与已知一致才允许跑实验格；对照格失败 → 新结构与原实现的差异不止设计维度，
  实验无效
- **反例**：2026-09-09 一天内两次：①级别 1 v1 手写两层 PCN，全 BP 基线卡 unigram
  （基线格失效）；②门来源 2×2 的 Hybrid 块四格全败含已知 ✅ 格——遗漏 gate_ln
  （教训 1 修复轮就发现的死锁条件）且值对象误用。两个实验各浪费一轮夜/日跑
- **要点**：**简化结构 = 未声明的多变量改动**。「只改一个维度」的实验在自建结构上
  几乎必然是谎言——正确做法是给原实现加开关参数，或先花十分钟校准对照格

### 8. 对照的超参必须对称调优——单边调优的基线是最隐蔽的假优势（2026-09-08，V5 期间）
- **情境**：任何「新架构 vs 基线」的对比实验，超参（尤其 lr）只按一边的最适值设置
- **必须先做**：两边各自做同规模的超参扫描（至少 lr 三点），用**各自最优**对比；或在报告中显式声明超参不对称及其方向
- **反例**：2026-09-08，因果重验后报告 PCN「+56.8%/+69.3% 优势」——全部基于 lr=3e-4，而这恰是 PCN 的最优区间、Transformer 的次优区间（Transformer@1e-3 = 15.14 直接打平反超）。教训 4（超参主导架构）的第三次应验，且是最隐蔽的一次：因果作弊已排除、5-seed 统计极稳，仍被超参不对称骗过
- **要点**：**统计稳定性（多 seed、小方差）不能补偿系统性偏差**。被排除的缺陷通道清单每加一项，「优势」才可信一分；疑点验证（基线复检/超参对称）必须先于结论发布，而不是之后
- **补充实例（2026-09-11，V6 系列）**：demo 与骨架系列全部用 `wt_causal_transformer_s0`（lr 3e-4 次优，PPL 557）作 TF 侧，得出「TF 微调退化 -19.7%」——公平版 TF（lr1e-3，PPL 215）同协议实为 **+5.4% 改善**。「TF 过拟合退化」整个叙事是次优基线伪影。同一轮还发现公平复核的价值：ΔW 约束幅度结论在换公平对后依然成立（1.91× vs 1.88×）——**基线勘误不一定推翻结论，但必须先勘误再引用**

### 10. 合成基准的「高于随机」必须先过启发式天花板自查（2026-09-11，V6 系列）
- **情境**：自建合成任务（MQAR/复制/绑定类）上某臂准确率「高于随机基线」
- **必须先做**：在**固定测试集上直接计算平凡启发式的得分天花板**——众数值/多数类/
  最近重复/unigram 等零智能策略的准确率；臂的得分 ≤ 天花板 → 信号被捷径解释，不是能力证据
- **反例**：2026-09-11 MQAR 阶段一~三，门控臂 n=16 得 0.115（随机 0.0156，7 倍）被当作
  「门控通路有联想召回」的方向性证据；混淆排查发现值有放回采样下「预测众数值」
  启发式在同一测试集得 **0.1263 > 0.115**——信号被完全解释。且臂得分随 n 的下降
  （0.115/随机/随机）与启发式天花板随 n 的下降（0.126/0.089/0.068）模式吻合
- **要点**：「高于随机」≠「学会了任务」。合成任务的采样方式决定了平凡捷径的
  存在与否（有放回→频率捷径；查询位与键位过近→位置捷径）。**先算捷径天花板，
  再解读信号**；采样应优先无放回/去捷径构造

---

## V4 设计约束（基于教训）

1. **不从 Transformer 出发** — 选一个根本不同的计算基元
2. **先定义"什么算成功"** — 在动手写代码前，明确什么样的结果能证明"这不是 Transformer 的变体"
3. **最小可区分实验** — 设计能直接区分"新机制有效" vs "只是正则化"的实验
4. **接受更大规模** — 如果验证需要比 12 层 d=256 更大的模型，就做更大的模型

---

*2026-06-24 整理自 PRISM V1-V3（2026-06-07 ~ 2026-06-24，18 天，20+ 实验）*

### 10. 对照实验的 checkpoint 必须来自同一预训练域（2026-09-16，V7-A4）
- **情境**：比较不同架构变体在微调任务上的表现
- **必须先做**：确认所有对照组的预训练语料一致；如果需要跨域比较，
  必须有同域对照组来拆解「域效应」与「架构效应」
- **反例**：2026-09-16 A4，稠密 PCN 用 WikiText checkpoint、稀疏 PCN 用
  TinyStories checkpoint → 域效应（跨域 vs 域内）被误判为稀疏效应，
  得出「稀疏摧毁冷启动」的错误结论。四臂对照修正后：稀疏不损害冷启动
  （+57.9% vs +53.2%），两者完全兼容
- **要点**：这是第五次测量偏差被用户/预设程序捕获。对照不只是「同预算」
  ——还需要「同域、同预训练协议」。域是隐藏变量中最容易遗漏的
